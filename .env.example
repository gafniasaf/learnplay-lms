# API Configuration
# Set to "true" to use mock data, "false" to use Supabase
VITE_USE_MOCK=true

# MCP Proxy Configuration (for local development with MCP server)
# Set to "true" ONLY if you have the MCP proxy running at localhost:4000
# Default: false (uses Supabase Edge Functions directly)
VITE_USE_MCP_PROXY=false

# Supabase Configuration (automatically set by Lovable Cloud)
VITE_SUPABASE_URL=https://xlslksprdjsxawvcikfk.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inhsc2xrc3ByZGpzeGF3dmNpa2ZrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM3MTMxMzEsImV4cCI6MjA3OTI4OTEzMX0.1Jo8F2o42z_K7PXeHrEp28AwbomBkrrOJh1_t3vU0iM
VITE_SUPABASE_PROJECT_ID=xlslksprdjsxawvcikfk
VITE_ENABLE_DEV=true

# Fallback Configuration
# Set to "true" to allow falling back to mock data if live API fails
VITE_ALLOW_MOCK_FALLBACK=true

# Preview Configuration
# Set to "true" to read catalog/courses directly from Storage (bypasses edge functions)
VITE_USE_STORAGE_READS=true

# Embed Mode Configuration
# Comma-separated list of allowed parent origins for secure postMessage communication
# Example: VITE_EMBED_ALLOWED_ORIGINS=https://lms.example.com,https://school.example.org
VITE_EMBED_ALLOWED_ORIGINS=
