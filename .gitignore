# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated reports and artifacts
reports/
artifacts/
coverage/
reports/playwright-html/
playwright-report/
# Local caches / state
.cache/
# Test result snapshots
junit.xml
# Jest/Playwright outputs
test-results/
playwright/.auth/
ignite-zero-release.zip
public/ignite-zero-release.zip

# Factory artifacts
cursor-playground/golden-packs/
cursor-playground/reviews/
projects/*
!projects/.gitkeep

# Setup flags
.setup_complete
cursor-playground/workspaces/

# Archived legacy code
_archive/

# Local book image dumps (very large; never commit)
/books/
extracted_images.zip

# Updated images package (very large; never commit)
Updated images.zip
/Updated images/

# Local canonical JSON dumps (very large; never commit)
/canonicaljsonsfrommacbook/

# Local book render artifacts / previews
tmp/

# Local deliverables exports (very large; never commit)
/deliverables/
/deliverables.zip
/deliverables_v2_pdfs/
/Captured highres screenshots of highres indesign pdfs/
/receive-deliverables.mjs

# Local-only Docker build inputs (NEVER commit binaries like Prince installers)
book-worker/private/

# Environment files with secrets
learnplay.env
*.env
.env.learnplay.generated
!.env.example
!.env.local.example
!.deploy.env.example
!mes-migration.env.example

# Local scratch / imported dumps (NEVER commit)
/external/
/figures/
*.rar